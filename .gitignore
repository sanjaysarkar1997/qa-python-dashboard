# Virtual environment — never commit this to Git
.venv/*
venv/
env/

# Python cache files
__pycache__/
*.pyc
*.pyo

# IDE settings
.vscode/
.idea/

# macOS junk
.DS_Store

# Environment variables — contains secrets / local paths
# .env.example is safe to commit (no real values), .env is NOT
.env
